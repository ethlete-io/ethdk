{
  "$schema": "http://json-schema.org/schema",
  "$id": "migrate-to-contentful-v5",
  "title": "Migrate to @ethlete/contentful v5",
  "type": "object",
  "properties": {
    "projects": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Nx projects to migrate. Defaults to every project in the workspace. Every site is rewritten independently, so any scope is safe — use this to keep the diff reviewable.",
      "alias": "project"
    },
    "include": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Path prefixes to migrate, relative to the workspace root (e.g. 'libs/ui'). Unioned with `projects`. Defaults to the whole workspace.",
      "alias": "includePath"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files after migration",
      "default": false
    }
  },
  "required": []
}
