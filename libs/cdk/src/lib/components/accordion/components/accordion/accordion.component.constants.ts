import { InjectionToken } from '@angular/core';
import { AccordionComponent } from './accordion.component';

export const ACCORDION_COMPONENT = new InjectionToken<AccordionComponent>('AccordionComponent');
