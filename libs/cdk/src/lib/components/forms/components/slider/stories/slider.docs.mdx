# Slider

The slider component is used to select a value from a range of values.
Imported by adding the `SliderImports` array to the `imports` array of your standalone component.

## Usage

```html
<et-slider-field [formControl]="fg">
  <et-label>Slider</et-label>
  <et-slider />
</et-slider-field>
```
