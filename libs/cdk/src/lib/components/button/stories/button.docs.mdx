import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="CDK/Buttons/Button" />

# Button

A directive that can be added to `<button>` and `<a>` elements to provide additional behavior.

## Usage

```html
<button et-button>Button</button>

<a et-button>Link</a>
```
