import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="CDK/Query Error" />

# Query error

Display the error response of a failed query. Will either display a singe error as a p tag or a ul list of errors.

```html
<et-query-error *ngIf="error" [error]="error" />
```
