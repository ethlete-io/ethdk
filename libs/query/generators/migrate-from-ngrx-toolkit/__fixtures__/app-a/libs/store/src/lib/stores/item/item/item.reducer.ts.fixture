import { Action } from '@ngrx/store';
import { createReducerSlice } from '@tomtomb/ngrx-toolkit';
import * as Actions from './item.actions';

export const ITEM_FEATURE_KEY = 'item';

export type State = typeof initialState;

export interface ItemPartialState {
  readonly [ITEM_FEATURE_KEY]: State;
}

export const { reducerAdapters, reducerSlice, initialState } = createReducerSlice({
  actions: Actions.ITEM_ACTIONS,
  key: ITEM_FEATURE_KEY,
  initialStateExtra: {},
});

export function reducer(state: State | undefined, action: Action) {
  return reducerSlice(state, action);
}
