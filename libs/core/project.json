{
  "name": "core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/core/src",
  "prefix": "ethlete",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "dependsOn": ["^build", "build-generators", "generate-version"],
      "outputs": ["{workspaceRoot}/dist/libs/core"],
      "defaultConfiguration": "production",
      "options": {
        "project": "libs/core/ng-package.json",
        "tsConfig": "libs/core/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/core/tsconfig.lib.prod.json"
        },
        "development": {}
      }
    },
    "test": {
      "outputs": ["{projectRoot}/{options.coverage.reportsDirectory}", "{workspaceRoot}/coverage/libs/core"],
      "options": {
        "coverage.reportsDirectory": "../../../coverage/libs/core"
      }
    },
    "lint": {
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/.eslintrc.json",
        "{workspaceRoot}/.eslintignore",
        "{workspaceRoot}/eslint.config.mjs",
        "{workspaceRoot}/tools/eslint-rules/**/*",
        {
          "externalDependencies": ["eslint"]
        }
      ]
    },
    "build-generators": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["rm -rf libs/core/generators/dist", "tsc -p libs/core/generators/tsconfig.json"]
      }
    },
    "generate-version": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node tools/scripts/generate-version.js core"]
      }
    }
  }
}
