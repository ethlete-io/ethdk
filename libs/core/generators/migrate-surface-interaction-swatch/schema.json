{
  "$schema": "http://json-schema.org/schema",
  "$id": "migrate-surface-interaction-swatch",
  "title": "Migrate surface interactionColor to the swatch shape",
  "type": "object",
  "properties": {
    "projects": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Nx projects to migrate. Defaults to every project in the workspace.",
      "alias": "project"
    },
    "include": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Path prefixes to migrate, relative to the workspace root (e.g. 'apps/web'). Unioned with `projects`. Defaults to the whole workspace.",
      "alias": "includePath"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files after migration",
      "default": false
    }
  },
  "required": []
}
