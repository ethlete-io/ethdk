import { Component, ViewEncapsulation } from '@angular/core';

/**
 * The waterfall's grid, axis and bars, as a styles-only component the panel mounts when the Timeline tab
 * is first opened.
 *
 * @internal
 */
@Component({
  selector: 'et-query-devtools-timeline-styles',
  template: '',
  styleUrl: './query-devtools-timeline-styles.component.css',
  encapsulation: ViewEncapsulation.None,
})
export class QueryDevtoolsTimelineStylesComponent {}
