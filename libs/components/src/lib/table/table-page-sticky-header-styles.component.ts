import { Component, ViewEncapsulation } from '@angular/core';

/**
 * The split page-sticky header layout, as a styles-only component mounted by `etTablePageStickyHeader` - see
 * `TableDragScrollStylesComponent` for the same pattern.
 *
 * @internal
 */
@Component({
  selector: 'et-table-page-sticky-header-styles',
  template: '',
  styleUrl: './table-page-sticky-header-styles.component.css',
  encapsulation: ViewEncapsulation.None,
})
export class TablePageStickyHeaderStylesComponent {}
