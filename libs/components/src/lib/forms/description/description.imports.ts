import { DescriptionComponent } from './description.component';

/** The muted `et-description` block for secondary copy next to a control or a label. */
export const DESCRIPTION_IMPORTS = [DescriptionComponent] as const;
