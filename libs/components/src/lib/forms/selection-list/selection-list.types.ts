/**
 * How a checkbox or radio group lays its options out. Vertical (the default) scans better and gives
 * each option a full-width hit area; horizontal suits a small set of short options - a two-way filter,
 * a size picker - and wraps rather than overflowing.
 */
export type SelectionListOrientation = 'vertical' | 'horizontal';
