import { Component, ViewEncapsulation } from '@angular/core';

/**
 * The bottom-sheet presentation of the cascader panel, as a styles-only component mounted the first
 * time the panel is shown below the `md` breakpoint.
 *
 * @internal
 */
@Component({
  selector: 'et-cascader-sheet-styles',
  template: '',
  styleUrl: './cascader-sheet-styles.component.css',
  encapsulation: ViewEncapsulation.None,
})
export class CascaderSheetStylesComponent {}
