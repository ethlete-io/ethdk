import '../../../test-helpers';
import { PipPlayerComponent } from './pip-player.component';

describe('PipPlayerComponent', () => {
  it('component is defined', () => {
    expect(PipPlayerComponent).toBeDefined();
  });
});
