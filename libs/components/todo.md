- generally across multiple form components; the selection indicator should be on the right, not the left (checkmark and radio dots as well as custom icons)
- grid item movement is not possible with touch, only mouse, resize works though
- the color theme generator needs some sort of extension on how to set a specific theme as the default during generation. the current setup has problems in mono repos where one set of theme definitions is shard across the repo but apps require different themes as default. same applies to the surface theme generator
- if a ProvideColorDirective gets added via hostDirective on the app component, injected and set via .forceColor i would expect overlays to automatically get the same theme but that does not happen. There seems to be a bug on how the color theme to use for the overlay gets resolved (DI wont work but we already have a system for that in place via sync)
- cascader animations on the non bottom sheet ui need improvement. each added or removed section should grow/shrink on open/close in the logical navigation direction.
- cascader with eg. 6 sub levels needs testing. at some point it should probably become horizontally scrollable instead of getting extremely long or even overflowing the viewport. Or maybe the ui transforms and after eg. 3 section the first section gets rendered as a tab above so the user can go back there quickly. Horizontal scrolling on pc can become tedious quickly. Though cursor drag scroll could help or such landmark elements like breadcrumbs
- we should also have a query adapter for the legacy v2 query system for async selects
- entering search mode in cascader coming from a existing eg. 3 part selection resizes the overlay in width and height without animation
- cascader error ui needs improvements
- date range masked mode missing
- selecting a date in date time input auto selects the current time
- duration input masked mode missing
- select with max selection should probably disable all options once max selection is reached
- virtual scroll (select story with many options) does not work, all items get rendered in dom
- time input masked mode missing
- grid item movement is sometimes hard. (Story Default) Drag the top left chart widget downwards. You will need to drag it all the way below the text widget to cause actual repositioning. this is probably because theres not enough space in the second col since the text widget block is smaller than the chart widget
