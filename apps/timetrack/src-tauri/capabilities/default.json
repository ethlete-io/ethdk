{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the app window may reach. Every provider call goes through this app's own commands, so no HTTP or shell plugin permission belongs here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
