[package]
name = "ethlete-studio"
version = "0.1.0"
description = "A local decision workspace for agent-assisted design work"
edition = "2021"
publish = false

[lib]
name = "ethlete_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [] }
