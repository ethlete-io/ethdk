---
'@ethlete/timetrack': minor
'@ethlete/agent-rules': minor
---

A new Start view files a ticket, creates the branch the grammar names for it and opens a draft merge
request. `featureBranchesFor` and `nestedSpecFor` are now shared with the CLI.
