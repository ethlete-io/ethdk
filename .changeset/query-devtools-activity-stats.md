---
'@ethlete/components': minor
'@ethlete/query': minor
---

Query devtools: count what every query cost - how often it refreshed, how many of those refreshes
reached the network, and how much payload they moved - shown as activity tiles per query, a size per
list row, a total per client and a traffic row on stack and sequence cards.
