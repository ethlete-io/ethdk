---
'@ethlete/timetrack': patch
---

The v2 day reads the editor heartbeats a reporter posts: they name the checkout a window title reading `Visual Studio Code` cannot, and they add no presence of their own. A meeting title is no longer quotable evidence, so it never reaches a ticket description or a prompt to an agent CLI.
