---
'@ethlete/core': minor
---

Add `provideAppUpdates()`, which stops a tab that was open across a deploy from breaking on a lazy chunk that no longer exists - reloading on its own when no form would lose changes.
