---
'@ethlete/components': patch
---

Table: keep a `etTablePageStickyHeader` table narrowing with its container instead of scrolling the page sideways, end the pinned header on its own rule, and leave its scrollbar to the host.
