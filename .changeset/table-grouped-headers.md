---
'@ethlete/components': minor
---

Table: grouped headers. A `group` on a column def renders adjacent columns sharing
it beneath one spanning label in a second header row; each sub-column stays an
ordinary column (independently sortable, filterable, reorderable), and both header
rows stay pinned while scrolling. Columns without a `group` span both rows.
