---
'@ethlete/query': patch
---

Legacy `QueryForm`: keep a committed value's type through its own URL write, leave a param mirrored with `appendToUrl: false` alone, and honour `resetAllFieldsToDefault({ skipFields })` against `isResetBy`.
