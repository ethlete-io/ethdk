---
'@ethlete/query': patch
'@ethlete/query-devtools': patch
---

Auth multi-tab sync: a tab the browser froze now gives the leadership up instead of holding it while it refreshes nothing, and a follower whose token goes stale takes the refresh over when the leader does not answer.
