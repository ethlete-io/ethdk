---
'@ethlete/components': minor
---

Add `et-kbd`, which renders a shortcut such as `keys="mod+k"` as keycaps using the current platform's glyphs - `⌘ K` on Apple, `Ctrl K` elsewhere. Pin it with the `platform` input or `KBD_PLATFORM`.
