---
'@ethlete/components': minor
---

Stream: the PiP window's title bar is now a tab stop - the arrow keys move the window and `Enter`/`Space` brings a collapsed one back into view, labelled by the new `STREAM_LABELS.pipMove`.
