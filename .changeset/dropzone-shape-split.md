---
'@ethlete/components': patch
---

Dropzone: split the readonly and single-file preview CSS into styles-only components mounted only when they render, and moved the multiple-mode list item onto its own `et-dropzone-item` child.
