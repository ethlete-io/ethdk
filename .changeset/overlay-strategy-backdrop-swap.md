---
'@ethlete/core': major
'@ethlete/components': patch
---

An overlay strategy switch between a backdropped and a backdrop-less shape now adds or
removes the backdrop instead of keeping the one it mounted with. `elements.backdropElement`
is a signal.
