---
'@ethlete/components': minor
---

Buttons: `tone` and `mutedUntilPressed` are replaced by `color="surface"` and `pressedColor`, so neutral toning is a color theme rather than a per-component override.
