---
'@ethlete/components': patch
---

Grid items draw a keyboard focus ring, and a standalone chip's remove button is a tab stop - `CHIP_REMOVE_TAB_STOP` keeps it out of the tab order inside widgets that manage chip focus themselves.
