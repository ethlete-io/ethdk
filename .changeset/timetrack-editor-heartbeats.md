---
'@ethlete/timetrack': minor
---

Editor heartbeats: a new `editor` source names the checkout, branch and directory being edited, and
an `ingest` module carries the wire format a reporter posts to the app's local endpoint.
