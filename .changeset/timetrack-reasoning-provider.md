---
'@ethlete/timetrack': minor
---

A day review can now ask your own `claude` CLI what the work it could not name belongs to. The answer
arrives as a `weak` suggestion with its reason attached, so nothing syncs unread.
