---
'@ethlete/components': minor
---

Forms: add `colorContrast(path, { against })`, which validates a color field against another field of
the same form, plus the `getColorContrastRatio()` helper and `WCAG_CONTRAST_RATIOS`.
