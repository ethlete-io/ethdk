---
'@ethlete/components': minor
---

Buttons: `tone="surface"` colors a surface or icon button from the surface it sits on instead of the
ambient color theme, so a secondary action reads as chrome without a neutral color theme registered.
