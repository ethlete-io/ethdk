---
'@ethlete/cli': minor
---

`et api help <name>` now answers for one API: the commands it accepts, what each one runs, its url
and services, and the state of its checkout. `--help` after a known API name prints the same text.
