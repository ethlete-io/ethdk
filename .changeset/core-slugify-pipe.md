---
'@ethlete/core': minor
---

Add a `slugify` util and `SlugifyPipe` (`| slugify`) that convert a string into a URL-friendly slug — diacritics stripped, lowercased, non-alphanumeric runs collapsed to single hyphens.
