---
'@ethlete/components': minor
---

Add `<et-query-devtools>` (`QUERY_DEVTOOLS_IMPORTS`): a floating, dockable panel
that inspects the signals-first `@ethlete/query` system — queries, stacks,
sequences, bearer auth providers, the repository cache and a rolling event log.
Enable instrumentation with `provideQueryDevtools()` from `@ethlete/query`.
