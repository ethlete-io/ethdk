---
'@ethlete/components': patch
---

Table: with `etTableKeyboardNav`, `Enter` on a cell holding a control only drills into it; a `rowInteractive` row's `rowClick` now fires from `Enter` only on a cell with nothing of its own to open.
