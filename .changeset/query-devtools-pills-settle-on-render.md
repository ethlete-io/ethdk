---
'@ethlete/query': patch
---

Query devtools: the floating pills now take themselves off a page with no devtools UI shortly after the first paint, instead of staying forever in an app that polls or holds a session.
