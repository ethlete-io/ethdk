---
'@ethlete/agent-rules': minor
---

Generated files no longer stamp the package version into their banner, so a release bump alone no longer reports every repo as out of sync - only real content changes do.
