---
'@ethlete/components': minor
---

`et-badge` gained a `size` input (`sm | md | lg`) and an icon slot: an element carrying `etIcon` is sized to the badge's font size, on either side of the label via `iconAlignment`.
