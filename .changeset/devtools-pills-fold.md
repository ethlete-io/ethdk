---
'@ethlete/query': minor
---

The devtools' floating API env and session pickers are now folded into one small chip, which reads
the current values and unfolds every picker on a click. A production pick keeps them unfolded.
