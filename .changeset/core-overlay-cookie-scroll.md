---
'@ethlete/core': patch
---

A closing overlay no longer pulls focus back when the user has already clicked elsewhere, the cookie helpers no-op instead of throwing on the server, and `onPathParamChange` only skips scroll-to-top within the same route.
