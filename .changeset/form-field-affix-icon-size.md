---
'@ethlete/components': minor
---

Form field: an `[etIcon]` projected into `[etInputPrefix]` / `[etInputSuffix]` is now sized by the
shell (`--et-form-field-affix-icon-size`, `16px`) — no size class needed, matching the other
in-field icons.
