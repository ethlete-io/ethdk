---
'@ethlete/agent-rules': minor
---

Add the git-flow branch convention: a `git-flow` skill and an `ethlete-agents git-flow check|explain` command, both driven by one `gitFlow` config block, plus the parser at `@ethlete/agent-rules/git-flow`.
