---
'@ethlete/components': minor
---

Nav tabs: a router-driven tab link no longer selects itself on click. The underline stays on the page currently rendered while a navigation guard is deciding, moves once the guard lets the navigation through, and never moves when one refuses.
