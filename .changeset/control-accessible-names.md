---
'@ethlete/components': patch
---

Every form control now takes `aria-label` / `aria-labelledby` and forwards it onto the element carrying its role, a required rich text editor announces `aria-required`, an option's `et-description` is announced, and a segmented button names itself.
