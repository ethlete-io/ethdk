---
'@ethlete/timetrack': minor
---

Add `streamDay()`: what a local day was worked on, for how long and what the agents spent, with one
line per checkout and every other application folded into one. `formatTokenCount()` reads its spend.
