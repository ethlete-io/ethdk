---
'@ethlete/components': major
---

The rich text editor's heading, quote, code-block, link and autoformat behaviour is now opt-in - add
`provideRichTextEditorDefaultTools()` to keep the previous toolbar. Saves 3.5 kB gz without it.
