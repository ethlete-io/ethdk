---
'@ethlete/components': minor
---

Scheduler: appointment badges are now built from composable adornments (title, time range, color dot, location, sub-appointment count) - disable any one with its own `etSchedulerBadge*` config on `<et-scheduler>`. `Appointment` gains an optional `location` field.
