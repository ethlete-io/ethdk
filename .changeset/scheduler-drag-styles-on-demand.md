---
'@ethlete/components': patch
---

Scheduler: the time grid's drag cursor, dragging shadow and resize handles now live in a styles-only component mounted only while appointment dragging is enabled, instead of always shipping with the time grid; no visual or API change.
