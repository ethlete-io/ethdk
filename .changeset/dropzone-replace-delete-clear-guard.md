---
'@ethlete/components': patch
---

Dropzone: replacing the file in single mode now fires the configured `delete` for the value it drops, `clear()` respects `readonly`/`disabled`, and the live region uses `DROPZONE_LABELS.uploading`.
