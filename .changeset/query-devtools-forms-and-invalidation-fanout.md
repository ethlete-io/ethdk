---
'@ethlete/components': minor
'@ethlete/query': minor
---

Query devtools: add a **Forms** tab covering `createQueryForm` - its fields, URL params and the query it drives - and log each invalidation with every query it refetched.
