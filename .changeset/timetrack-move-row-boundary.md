---
'@ethlete/timetrack': minor
---

Add `moveRowBoundary()`, which moves the instant two adjacent worklog rows meet at without changing
the pair's total, so a split can be placed exactly rather than only halved.
