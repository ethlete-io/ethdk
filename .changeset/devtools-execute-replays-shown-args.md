---
'@ethlete/components': patch
---

Query devtools: **Execute** replays the args the panel is showing, so a query executed imperatively no longer replays empty - a function route used to throw `ET003` into the app's `ErrorHandler`.
