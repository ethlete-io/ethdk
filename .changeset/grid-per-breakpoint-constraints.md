---
'@ethlete/components': major
---

Grid items take span bounds per breakpoint, through `constraints.perBreakpoint` on a registration or
`[perBreakpointConstraints]` on `et-grid-item`. `getConstraintsForColumns()` is now
`getConstraintsForBreakpoint()`.
