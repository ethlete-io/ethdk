---
'@ethlete/cli': minor
---

`et api up` now refuses a port another program holds and prints a per-service state table instead of
the engine's output. New `et api clear` removes a managed checkout.
