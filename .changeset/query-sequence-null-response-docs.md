---
'@ethlete/query': patch
---

Docs: a `querySequence` step answering `204` passes `null` to the next `mapArgs`, and `QueryFieldOptions.appendToUrl` now describes what the legacy `QueryForm` actually writes.
