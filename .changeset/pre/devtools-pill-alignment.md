---
'@ethlete/query': patch
---

Query devtools: the floating pills now share one height, one set of edges and one set of picker
columns, and the fold control reads as a button instead of a lone caret.
