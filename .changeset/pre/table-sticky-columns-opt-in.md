---
'@ethlete/components': major
---

Table: sticky columns are now the opt-in `etTableStickyColumns` (`TABLE_STICKY_COLUMNS_IMPORTS`), taking 386 B gz off a plain table and its measuring off every resize. A column's `sticky` renders unpinned without it.
