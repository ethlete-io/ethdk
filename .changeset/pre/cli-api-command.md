---
'@ethlete/cli': minor
---

Add `et api` to run an app's backend from a local checkout, declared in `ethlete.apis.js`. The new
`ethlete.config.local.json` says where the sibling checkouts on this machine live.
