---
'@ethlete/query-devtools': minor
---

A Batches tab inspects a `createQueryBatch` run - progress, throughput, time remaining and every
item's args and outcome - and the Queries list folds a run's items under one row.
