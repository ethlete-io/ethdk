---
'@ethlete/eslint-plugin': minor
---

New rule `ethlete/no-async-await`, replacing the `no-restricted-syntax` selectors that could not be
toggled on their own. It also catches bare `await` and `for await…of`.
