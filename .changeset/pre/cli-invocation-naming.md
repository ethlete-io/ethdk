---
'@ethlete/cli': patch
---

Every `et` message now names a command the reader can type, for example `yarn api` or `npx et api`,
in place of a bare `et`. `et doctor` names the `et api` command that fixes each problem.
