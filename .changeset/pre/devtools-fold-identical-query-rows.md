---
'@ethlete/components': patch
---

Query devtools: the Queries list folds rows it would otherwise repeat - one query used by several consumers is now one line with an instance count, expandable to reach each consumer.
