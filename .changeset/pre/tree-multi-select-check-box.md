---
'@ethlete/components': patch
---

Tree: a multi selectable tree states selection with a leading check box instead of a fill, so adjacent selected rows no longer paint one continuous accent slab. Single select is unchanged.
