---
'@ethlete/components': patch
---

Menu: a scrolling option list is cut inside the panel instead of on the search field's border, and its cut edges fade while more can be scrolled. Size the fade with `--et-menu-scroll-fade-size`.
