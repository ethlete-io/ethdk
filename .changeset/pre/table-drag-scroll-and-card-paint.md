---
'@ethlete/components': minor
---

Table: add `etTableDragScroll` (`TABLE_DRAG_SCROLL_IMPORTS`), which pans a wide table by dragging anywhere in it, and `<et-table-column-chooser>` takes a `variant` for its trigger.
