---
'@ethlete/components': patch
---

The menu, cascader, time picker and docked rich text editor toolbar now show `<et-scrollbar>` instead
of a native or hidden one, and pressing a scrollbar no longer moves focus.
