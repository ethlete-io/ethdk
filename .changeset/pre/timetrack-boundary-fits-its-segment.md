---
'timetrack-app': patch
---

The handle between two rows on the day screen spans only the stretch of the lane the two rows are
drawn in, so its bar and its time no longer lie over a row beside them where the lane is split.
