---
'@ethlete/components': major
---

A loading button keeps DOM focus and its place in the tab order. Native `disabled` and
`tabindex="-1"` now come from `disabled` alone, and the directive blocks the click instead.
