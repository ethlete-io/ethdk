---
'@ethlete/query': patch
'@ethlete/query-devtools': patch
---

Query devtools: list a query's request headers in their own section, so a secure query's `Authorization` is readable however its args reached it.
