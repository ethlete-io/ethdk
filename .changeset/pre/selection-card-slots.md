---
'@ethlete/components': minor
---

Selection card: project a mark and a price into the ends with `[etSelectionCardLeading]` / `[etSelectionCardTrailing]`, and move the control with `controlPosition`.
