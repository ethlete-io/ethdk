---
'@ethlete/query-devtools': patch
---

Fixes the layout of a narrow panel: the Cache table scrolls in its own box, the Events base URL no
longer wraps one character per line, and every drawer tab's divider resizes its panes.
