---
'@ethlete/query': minor
'@ethlete/query-devtools': minor
---

The devtools Auth tab now keeps every session you log in as and switches between them, logs in as an
account declared through `provideQueryDevtools({ authAccounts })`, and can hold one tab's session
apart from the rest.
