---
'@ethlete/components': minor
---

Table: a column reorder no longer pans the table under itself when `etTableDragScroll` is on, and it auto-scrolls while held near an inline edge so a column can be dropped past the visible range.
