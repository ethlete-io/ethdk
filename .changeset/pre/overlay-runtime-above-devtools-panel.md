---
'@ethlete/core': patch
---

Overlay: raise the shared overlay root's z-index above the query devtools panel, so a menu/tooltip/dialog opened from inside the devtools (e.g. its response override menu) is no longer rendered behind the panel.
