---
'@ethlete/components': patch
---

The color picker's hex field is now an `et-form-field` with an `et-input`, so it carries the library's hover, focus and disabled treatment instead of a hand-rolled border.
