---
'@ethlete/components': patch
---

Scrollable: the buttons, dots, footer and edge-mask CSS now ships with the chrome that draws it, so a
track imported without `SCROLLABLE_NAVIGATION_IMPORTS` no longer pays for it.
