---
'@ethlete/components': patch
---

Query devtools: tapping a control no longer leaves it lit until something else is tapped, controls a hover would reveal are always visible on touch, and a sticky toolbar sticks flush to the top.
