---
'@ethlete/components': minor
---

Add `et-toolbar` / `[etToolbar]`: a bar of controls sharing one tab stop, navigated with the arrow keys per the ARIA toolbar pattern. The rich text editor's toolbar now uses it.
