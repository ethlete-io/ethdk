---
'@ethlete/components': patch
---

Notification: at `480px` and below the stack spans both edges and toasts fill it, instead of a `300px`-minimum card floating in a corner - which overflowed a `320px` viewport outright.
