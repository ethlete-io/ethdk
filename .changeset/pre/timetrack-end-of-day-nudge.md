---
'@ethlete/timetrack': minor
---

Say when a day is not finished: `dayNudge()` words the one reminder a day gets, from what the local
ledger says is still owed. `TimetrackSettings` gains a `nudge` field.
