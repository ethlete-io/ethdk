---
'@ethlete/components': minor
---

Query devtools: add a per-value override menu to the response explorer - edit, duplicate, or resize a value so it keeps reapplying on every future fetch - plus a "tampered" badge for overrides and armed faults.
