---
'@ethlete/components': minor
---

The component generator takes a `--category`, filing the new story under `Components/<Category>/<Name>` and matching the generated docs page's `<StoryEmbed>` id to it.
