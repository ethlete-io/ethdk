---
'@ethlete/components': patch
---

Tag input: a no-op removal no longer writes into the form model, a paste keeps the text
already in the field, and a full field stays editable while it holds text.
