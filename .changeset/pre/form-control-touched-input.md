---
'@ethlete/components': patch
---

Forms: marking a field touched programmatically (a `submit()` attempt, `markAsTouched()`) now shows
the error on `et-input`, `et-textarea`, `et-checkbox`, `et-switch` and the other wrapped controls.
