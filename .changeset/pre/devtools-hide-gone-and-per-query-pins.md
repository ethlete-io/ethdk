---
'@ethlete/components': patch
---

Query devtools: the Queries list hides destroyed queries until the **Gone** chip is on,
and a ★ pin now holds one row instead of every query of the same endpoint.
