---
'@ethlete/timetrack': minor
---

`sessionize` now follows the focused window's repository, so with several editor windows open on
different checkouts a title naming one re-points the block instead of leaving it on the branch last
committed to.
