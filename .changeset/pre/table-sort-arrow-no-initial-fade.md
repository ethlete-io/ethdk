---
'@ethlete/components': patch
---

Table: the sort arrow fades in only for a sort the reader just asked for. A sort that arrives from a URL, a restored state or a bound `rowsSource` now paints the arrow already in place.
