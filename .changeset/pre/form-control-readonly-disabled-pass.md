---
'@ethlete/components': minor
---

Readonly and disabled read the same way on every form control: readonly shows a plain cursor and
no hover, disabled shows `not-allowed`. `et-dropzone` gained a `readonly` input.
