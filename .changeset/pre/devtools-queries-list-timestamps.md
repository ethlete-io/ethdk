---
'@ethlete/components': patch
---

Query devtools: the Queries list carries a last-run time per row and sorts by it, newest first by default, with a control beside the search box that reverses the direction.
