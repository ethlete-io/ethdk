---
'@ethlete/components': minor
---

Table: a `cards` row keeps its rounded ends at a pinned column, the empty and error messages centre on the scroll viewport, and only a pinned block's outermost cell marks its edge.
