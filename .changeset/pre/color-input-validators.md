---
'@ethlete/components': minor
---

Forms: add the `hexColor` and `rgbColor` signal-forms validators, so a color value that arrives from an API or a paste still has to meet the format `et-color-input` documents.
