---
'@ethlete/agent-rules': minor
---

`sdkSourcePath`, `apiRepoPaths` and `apiRepoBranches` moved to `ethlete.config.local.json`, owned by
`@ethlete/cli`. The old file is still read, and reports where they went.
