---
'@ethlete/core': minor
---

Add `breakpointTransformBase`, `boolBreakpointTransform`, `numberBreakpointTransform` and `typedBreakpointTransform` for creating inputs that react to breakpoint changes if configured as such
