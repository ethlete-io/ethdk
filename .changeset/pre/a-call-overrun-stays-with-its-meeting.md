---
'@ethlete/timetrack': patch
'timetrack-app': patch
---

A call over several meetings is cut only between them. Time before the first meeting and after the
last one stays in that meeting's band, so a meeting that runs over is one band, not two.
