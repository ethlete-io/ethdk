---
'@ethlete/components': patch
'@ethlete/query': patch
---

Query devtools: a `null`/`undefined` value can now be overridden instead of showing an empty menu, and **Reset** appears only where something is armed and clears the whole subtree.
