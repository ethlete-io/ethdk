---
'@ethlete/components': major
---

Table: loading placeholders are now the opt-in `etTableSkeleton` (`TABLE_SKELETON_IMPORTS`), taking 999 B gz off a plain table. `loadingRows` becomes its `rows` option and `etTableCellSkeleton` moves into its imports array.
