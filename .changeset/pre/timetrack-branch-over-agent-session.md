---
'@ethlete/timetrack': patch
'timetrack-app': patch
---

A checkout's branch is taken from git and the editor wherever either has named one, so an agent
session that reports a stale branch no longer moves the checkout's time onto it.
