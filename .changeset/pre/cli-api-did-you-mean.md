---
'@ethlete/cli': patch
---

An unknown API name in `et api` now names the closest match and the APIs there are, in place of the
whole help text. An unknown command suggests the closest command the same way.
