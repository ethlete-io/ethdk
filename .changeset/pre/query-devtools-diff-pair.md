---
'@ethlete/components': minor
'@ethlete/query': minor
---

Query devtools: pick both ends of a response diff instead of only comparing a run against its predecessor, and raise how many bodies are kept with `provideQueryDevtools({ responseHistory })`.
