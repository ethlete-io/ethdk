---
'@ethlete/components': patch
---

Progress bar: `--et-progress-bar-height` and `--et-progress-bar-border-radius` now take effect when set on the element or an ancestor - they were registered as non-inheriting and silently kept their defaults.
