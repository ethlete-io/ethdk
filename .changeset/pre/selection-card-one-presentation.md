---
'@ethlete/components': major
---

The `variant="card"` chrome on `et-radio`, `et-checkbox-option` and `et-choice-field` is now one lazily-injected stylesheet with one token set. **Breaking:** `--et-radio-card-*`, `--et-checkbox-option-card-*` and `--et-choice-field-card-*` are now `--et-selection-card-*`.
