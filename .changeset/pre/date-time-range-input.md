---
'@ethlete/components': minor
---

Add `et-date-time-range-input`, a start/end control whose picker pairs a range calendar with `et-time-picker`'s new `mode="range"` - one set of columns holding both times.
