---
'@ethlete/timetrack': minor
---

Settings hold picked Jira projects (`favoriteProjects`) instead of typed key prefixes, an issue key
in free text is only read against them, and a reviewer can now write, move and remove a row the
collectors never saw.
