---
'@ethlete/components': minor
---

Table: `appearance="cards"` paints each row from the surface one elevation above the table's instead of tinting it, so a form field or `etAutoSurface` inside a card resolves its elevation from the card.
