---
'@ethlete/query': minor
'@ethlete/query-devtools': minor
---

The devtools session vault keeps one session per user on a backend that issues no `sub` claim, and
the panel logs in as an account with the body keys that login needs.
