---
'@ethlete/components': patch
---

Query devtools: **⌖ Locate** now finds a query created in a host element that renders no box of its own (`display: contents`) instead of reporting **Not on screen**.
