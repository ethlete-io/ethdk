---
'@ethlete/query': patch
---

Legacy query containers: destroying the last container of a polling query now stops the poll, and switching one container away no longer destroys a query another container still holds.
