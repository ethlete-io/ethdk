---
'@ethlete/components': minor
---

Progress steps: `state` gains `success`, `warning` and `error` - a step that finished with an outcome, each with its own icon and the app's matching semantic color theme.
