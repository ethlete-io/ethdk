---
'@ethlete/components': patch
---

Query devtools: the value explorer's and the Timeline's styles now load with those views, so the panel's stylesheet fits Angular's default `anyComponentStyle` budget again.
