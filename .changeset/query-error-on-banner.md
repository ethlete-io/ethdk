---
'@ethlete/components': major
---

Query error now renders as an `et-banner` of `type="error"`, themed with `--et-banner-*` - the `--et-query-error-*` tokens are gone. Banner gains `[etBannerHeading]` / `[etBannerBody]` slots and a `liveRegion` role override.
