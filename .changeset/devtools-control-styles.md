---
'@ethlete/query-devtools': patch
---

Query devtools: every action in the panel now reads as a button rather than plain text, text fields
and buttons no longer look alike, and one focus ring covers the whole panel.
