---
"@ethlete/cdk": minor
---

Add support for darkening non fully visible elements inside scrollable component via the `darkenNonIntersectingItems` property. The amount of darkening can be controlled via the `--darken-non-intersecting-items-amount` CSS property.
