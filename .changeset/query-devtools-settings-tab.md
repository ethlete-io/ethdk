---
'@ethlete/components': minor
'@ethlete/query': minor
---

Query devtools: a Settings panel behind the header's gear - a storage scope per kind of panel state, runtime limits, a reset, and every panel-wide switch in one place.
