---
'@ethlete/query': patch
---

Legacy: the request, auth and logger helpers the legacy barrel re-exports (`buildRoute`, `isNaN`, `decryptBearer`, ...) now carry the same `@deprecated` tag as the rest of the v2 API.
