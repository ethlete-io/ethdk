---
'@ethlete/query': patch
---

Query devtools: a second `provideQueryDevtools()` call on the same page now warns about the options it loses to the first call, and still takes part in the floating pills.
