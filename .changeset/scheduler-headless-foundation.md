---
'@ethlete/components': minor
---

Add the headless `SchedulerDirective` (`etScheduler`) - view/date state, per-view visible-range math, and sub-appointment chain building - as the foundation for an upcoming appointment calendar.
