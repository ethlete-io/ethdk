---
'@ethlete/components': patch
---

Bracket: `generateBracketDataForEthlete` accepts a stage whose leading rounds are drawn but empty, and range picker inputs no longer build their duplicate-field message on every registration.
