---
'@ethlete/components': patch
---

Calendar: a range preview now needs a real hover or keyboard focus in the selecting grid, so a strategy like `createFixedLengthRangeStrategy` no longer bands an untouched calendar.
