---
'@ethlete/core': major
'@ethlete/components': patch
'@ethlete/query-devtools': patch
---

- Notifications: a toast keeps one elevation, paints above overlays, and keeps out of every reserved viewport edge.
- Query devtools: a side dock reserves the edge it covers.
- `SurfaceContextTracker` drops `topType` / `topElevation`.
