---
'@ethlete/components': minor
---

Add the tree domain: `et-tree` and headless `[etTree]` render a hierarchy as an ARIA tree, loading each branch through a `TreeDataSource` the first time it expands.
