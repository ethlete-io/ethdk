---
'@ethlete/components': minor
---

Add `<et-scrollbar>`: a custom scrollbar for a container that already scrolls, on either axis, with a drag-to-scroll thumb, `autoHide` and right-to-left support.
