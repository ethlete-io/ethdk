---
'@ethlete/components': patch
---

Query devtools: a Queries-list group now opens once when the selection moves into it instead of being held open, so clicking its header collapses it even while it holds the selected query - matching how the tree view's path folders already behaved.
