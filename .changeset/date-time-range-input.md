---
'@ethlete/components': minor
---

Add `et-date-time-range-input`, a start/end control whose picker holds a range calendar plus the new `et-time-range-picker` (two time pickers as one, with a side-aware `timeFilter`). The scheduler's edit surface now uses it instead of two separate date-time inputs.
