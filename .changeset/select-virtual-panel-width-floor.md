---
'@ethlete/components': patch
---

Select: a content-sized panel (`[mirrorPanelWidth]="false"`) no longer resizes while scrolling a virtualized option list - it keeps the widest width it has needed until the search query changes.
