---
'@ethlete/query': minor
'@ethlete/query-devtools': minor
---

Query devtools: override an auth provider's access-token lifetime from the Auth tab, so the proactive refresh, the expiration warning and the logout behind them happen in seconds instead of hours.
