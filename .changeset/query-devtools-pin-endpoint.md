---
'@ethlete/components': minor
---

Query devtools: a **★** on each Queries row pins that endpoint to the top of the list. Pinning sorts rather than filters, so it composes with the client, search and status chips.
