---
'@ethlete/cdk': minor
---

Add support for breakpoint values in scrollable component inputs like `[itemSize]="{ xs: 'half', md: 'full' }"`. Normal values like `itemSize="half"` will still work as before, applying to all breakpoints.
