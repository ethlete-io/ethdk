---
'@ethlete/components': patch
---

Select: a projected `etSelectSearch` input without a placeholder of its own now shows the select's `placeholder`, and stops offering a caret and text cursor while the field is readonly.
