---
'@ethlete/timetrack': patch
---

A band now finds a person up to the day's `gapFillMs` either side of itself, so a short absence no longer reads `Nobody was here`. An idle transition answers one way only.
